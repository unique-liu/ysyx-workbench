From 6cc586d9e690b145a284eb3723a2cdc4de3b24d2 Mon Sep 17 00:00:00 2001
From: 2023k8009929022-LiuYaoqing <liuyaoqing23@mails.ucas.ac.cn>
Date: Sun, 5 Jul 2026 19:19:44 +0800
Subject: [PATCH 13/13] prepare for course

---
 perip/spi/rtl/spi_top_apb.v |   2 +-
 src/CPU.scala               |   4 +-
 src/Top.scala               |   2 +
 src/amba/APBDelayer.scala   |   2 +-
 src/amba/AXI4Delayer.scala  |   2 +-
 src/device/SPI.scala        | 275 ++++++++++++++++++------------------
 6 files changed, 144 insertions(+), 143 deletions(-)

diff --git a/perip/spi/rtl/spi_top_apb.v b/perip/spi/rtl/spi_top_apb.v
index ac68fd09..a745423c 100644
--- a/perip/spi/rtl/spi_top_apb.v
+++ b/perip/spi/rtl/spi_top_apb.v
@@ -1,6 +1,6 @@
 // define this macro to enable fast behavior simulation
 // for flash by skipping SPI transfers
-// `define FAST_FLASH
+`define FAST_FLASH
 
 module spi_top_apb #(
   parameter flash_addr_start = 32'h30000000,
diff --git a/src/CPU.scala b/src/CPU.scala
index 7559195f..fc4159c4 100644
--- a/src/CPU.scala
+++ b/src/CPU.scala
@@ -13,7 +13,7 @@ object CPUAXI4BundleParameters {
   def apply() = AXI4BundleParameters(addrBits = 32, dataBits = 32, idBits = ChipLinkParam.idBits)
 }
 
-class ysyx_00000000 extends BlackBox {
+class ysyx_26050152 extends BlackBox {
   val io = IO(new Bundle {
     val clock = Input(Clock())
     val reset = Input(Reset())
@@ -35,7 +35,7 @@ class CPU(idBits: Int)(implicit p: Parameters) extends LazyModule {
     val interrupt = IO(Input(Bool()))
     val slave = IO(Flipped(AXI4Bundle(CPUAXI4BundleParameters())))
 
-    val cpu = Module(new ysyx_00000000)
+    val cpu = Module(new ysyx_26050152)
     cpu.io.clock := clock
     cpu.io.reset := reset
     cpu.io.io_interrupt := interrupt
diff --git a/src/Top.scala b/src/Top.scala
index 2913155b..d133caaa 100644
--- a/src/Top.scala
+++ b/src/Top.scala
@@ -8,6 +8,8 @@ import freechips.rocketchip.diplomacy.LazyModule
 object Config {
   def hasChipLink: Boolean = false
   def sdramUseAXI: Boolean = true
+  def useFastFlash: Boolean = true
+  def useDelayer: Boolean = false
 }
 
 class ysyxSoCTop extends Module {
diff --git a/src/amba/APBDelayer.scala b/src/amba/APBDelayer.scala
index 6bbb4025..c2c721f6 100644
--- a/src/amba/APBDelayer.scala
+++ b/src/amba/APBDelayer.scala
@@ -26,7 +26,7 @@ class APBDelayerChisel extends Module {
   val s_power = 3 // power of s, use to fast divide
   val io = IO(new APBDelayerIO)
 
-  if(r == 0){
+  if(r == 0 || !Config.useDelayer){
     io.out <> io.in
   }else{
     val idle :: wait_finish :: wait_ret :: Nil = Enum(3)
diff --git a/src/amba/AXI4Delayer.scala b/src/amba/AXI4Delayer.scala
index b2ea7fc2..a806fee7 100644
--- a/src/amba/AXI4Delayer.scala
+++ b/src/amba/AXI4Delayer.scala
@@ -26,7 +26,7 @@ class AXI4DelayerChisel extends Module {
   val s_power = 3 // power of s, use to fast divide
   val io = IO(new AXI4DelayerIO)
 
-  if(r == 0){
+  if(r == 0 || !Config.useDelayer){
     io.out <> io.in
   }else{
     // read fsm
diff --git a/src/device/SPI.scala b/src/device/SPI.scala
index 585d14f3..2517d0b7 100644
--- a/src/device/SPI.scala
+++ b/src/device/SPI.scala
@@ -49,160 +49,159 @@ class APBSPI(address: Seq[AddressSet])(implicit p: Parameters) extends LazyModul
     mspi.io.in <> in
     spi_bundle <> mspi.io.spi
 
-    val normal :: xip_tx :: xip_div :: xip_ss :: xip_ctrl :: xip_wait :: xip_read :: xip_hold :: Nil = Enum(8)
-    val state = RegInit(normal)
-    val is_xip = Wire(Bool())
-    val xip_ready = RegInit(false.B)
+    if (!Config.useFastFlash) {
 
-    val idle :: setup :: enable :: get_data :: Nil = Enum(4)
-    val apb_state = RegInit(idle)
-    val apb_addr = RegInit(0.U(32.W))
-    val apb_rdata = RegInit(0.U(32.W))
-    val apb_wdata = RegInit(0.U(32.W))
-    val apb_write = RegInit(false.B)
-    val apb_run   = Wire(Bool())
-    val apb_bus   = Wire(new APBBundle(APBBundleParameters(addrBits = 32, dataBits = 32)))
-    
+      val normal :: xip_tx :: xip_div :: xip_ss :: xip_ctrl :: xip_wait :: xip_read :: xip_hold :: Nil = Enum(8)
+      val state = RegInit(normal)
+      val is_xip = Wire(Bool())
+      val xip_ready = RegInit(false.B)
+
+      val idle :: setup :: enable :: get_data :: Nil = Enum(4)
+      val apb_state = RegInit(idle)
+      val apb_addr = RegInit(0.U(32.W))
+      val apb_rdata = RegInit(0.U(32.W))
+      val apb_wdata = RegInit(0.U(32.W))
+      val apb_write = RegInit(false.B)
+      val apb_run   = Wire(Bool())
+      val apb_bus   = Wire(new APBBundle(APBBundleParameters(addrBits = 32, dataBits = 32)))
+      
 
-    is_xip := (in.paddr(29, 28) === "h3".U) && in.psel
-    assert(!(is_xip && in.pwrite && in.penable), "Can't write to flash")
+      is_xip := (in.paddr(29, 28) === "h3".U) && in.psel
+      assert(!(is_xip && in.pwrite && in.penable), "Can't write to flash")
 
-    apb_run := false.B
-    //input
-    mspi.io.in.paddr    := apb_bus.paddr
-    mspi.io.in.psel     := apb_bus.psel
-    mspi.io.in.penable  := apb_bus.penable
-    mspi.io.in.pprot    := apb_bus.pprot
-    mspi.io.in.pwrite   := apb_bus.pwrite
-    mspi.io.in.pwdata   := apb_bus.pwdata
-    mspi.io.in.pstrb    := apb_bus.pstrb
-    apb_bus.pready      := mspi.io.in.pready
-    apb_bus.prdata      := mspi.io.in.prdata
-    apb_bus.pslverr     := mspi.io.in.pslverr
-    //output
-    in.pready   := false.B
-    in.prdata   := 0.U(32.W)
-    in.pslverr  := false.B
-    switch(state) {
-      is(normal) {
-        when(is_xip && xip_ready) {
-          state := xip_ss
-          apb_run := true.B
-          apb_addr := 0x10001004.U(32.W)// tx1
-          apb_wdata := Cat(0x3.U(4.W),in.paddr(23, 0))
-          apb_write := true.B
-        }.elsewhen(is_xip && !xip_ready){
-          state := xip_tx
-          apb_run := true.B
-          apb_addr := 0x10001004.U(32.W)// tx1
-          apb_wdata := Cat(0x3.U(4.W),in.paddr(23, 0))
-          apb_write := true.B
-          xip_ready := true.B
-        }.otherwise {
-          mspi.io.in.paddr := in.paddr
-          mspi.io.in.psel := in.psel
-          mspi.io.in.penable := in.penable
-          mspi.io.in.pprot := in.pprot
-          mspi.io.in.pwrite := in.pwrite
-          mspi.io.in.pwdata := in.pwdata
-          mspi.io.in.pstrb := in.pstrb
-          in.pready := mspi.io.in.pready
-          in.prdata := mspi.io.in.prdata
-          in.pslverr := mspi.io.in.pslverr
-          xip_ready := false.B
+      apb_run := false.B
+      //input
+      mspi.io.in.paddr    := apb_bus.paddr
+      mspi.io.in.psel     := apb_bus.psel
+      mspi.io.in.penable  := apb_bus.penable
+      mspi.io.in.pprot    := apb_bus.pprot
+      mspi.io.in.pwrite   := apb_bus.pwrite
+      mspi.io.in.pwdata   := apb_bus.pwdata
+      mspi.io.in.pstrb    := apb_bus.pstrb
+      apb_bus.pready      := mspi.io.in.pready
+      apb_bus.prdata      := mspi.io.in.prdata
+      apb_bus.pslverr     := mspi.io.in.pslverr
+      //output
+      in.pready   := false.B
+      in.prdata   := 0.U(32.W)
+      in.pslverr  := false.B
+      switch(state) {
+        is(normal) {
+          when(is_xip && xip_ready) {
+            state := xip_ss
+            apb_run := true.B
+            apb_addr := 0x10001004.U(32.W)// tx1
+            apb_wdata := Cat(0x3.U(4.W),in.paddr(23, 0))
+            apb_write := true.B
+          }.elsewhen(is_xip && !xip_ready){
+            state := xip_tx
+            apb_run := true.B
+            apb_addr := 0x10001004.U(32.W)// tx1
+            apb_wdata := Cat(0x3.U(4.W),in.paddr(23, 0))
+            apb_write := true.B
+            xip_ready := true.B
+          }.otherwise {
+            mspi.io.in.paddr := in.paddr
+            mspi.io.in.psel := in.psel
+            mspi.io.in.penable := in.penable
+            mspi.io.in.pprot := in.pprot
+            mspi.io.in.pwrite := in.pwrite
+            mspi.io.in.pwdata := in.pwdata
+            mspi.io.in.pstrb := in.pstrb
+            in.pready := mspi.io.in.pready
+            in.prdata := mspi.io.in.prdata
+            in.pslverr := mspi.io.in.pslverr
+            xip_ready := false.B
+          }
         }
-      }
-      is(xip_tx) {
-        when(apb_state === idle || apb_state === enable && apb_bus.pready){
-          state := xip_div
-          apb_run := true.B
-          apb_addr := 0x10001014.U(32.W)// div
-          apb_wdata := 2.U(32.W)
-          apb_write := true.B
+        is(xip_tx) {
+          when(apb_state === idle || apb_state === enable && apb_bus.pready){
+            state := xip_div
+            apb_run := true.B
+            apb_addr := 0x10001014.U(32.W)// div
+            apb_wdata := 2.U(32.W)
+            apb_write := true.B
+          }
         }
-      }
-      is(xip_div) {
-        when(apb_state === idle || apb_state === enable && apb_bus.pready){
-          state := xip_ss
-          apb_run := true.B
-          apb_addr := 0x10001018.U(32.W)// ss
-          apb_wdata := 1.U(32.W)
-          apb_write := true.B
+        is(xip_div) {
+          when(apb_state === idle || apb_state === enable && apb_bus.pready){
+            state := xip_ss
+            apb_run := true.B
+            apb_addr := 0x10001018.U(32.W)// ss
+            apb_wdata := 1.U(32.W)
+            apb_write := true.B
+          }
         }
-      }
-      is(xip_ss) {
-        when(apb_state === idle || apb_state === enable && apb_bus.pready){
-          state := xip_ctrl
-          apb_run := true.B
-          apb_addr := 0x10001010.U(32.W)// ctrl
-          apb_wdata := 0x2140.U(32.W)
-          apb_write := true.B
+        is(xip_ss) {
+          when(apb_state === idle || apb_state === enable && apb_bus.pready){
+            state := xip_ctrl
+            apb_run := true.B
+            apb_addr := 0x10001010.U(32.W)// ctrl
+            apb_wdata := 0x2140.U(32.W)
+            apb_write := true.B
+          }
         }
-      }
-      is(xip_ctrl) {
-        when(apb_state === idle || apb_state === enable && apb_bus.pready){
-          state := xip_wait
-          apb_run := true.B
-          apb_addr := 0x10001010.U(32.W)// ctrl
-          apb_wdata := 0.U(32.W)
-          apb_write := false.B
+        is(xip_ctrl) {
+          when(apb_state === idle || apb_state === enable && apb_bus.pready){
+            state := xip_wait
+            apb_run := true.B
+            apb_addr := 0x10001010.U(32.W)// ctrl
+            apb_wdata := 0.U(32.W)
+            apb_write := false.B
+          }
         }
-      }
-      is(xip_wait) {
-        when((apb_state === idle) && (apb_rdata(8) === 1.U) || (apb_state === enable && apb_bus.pready && apb_bus.prdata(8) === 1.U)){
-          state := xip_wait
-          apb_run := true.B
-        }.elsewhen((apb_state === idle) && (apb_rdata(8) === 0.U) || (apb_state === enable && apb_bus.pready && apb_bus.prdata(8) === 0.U)){
-          state := xip_read
-          apb_run := true.B
-          apb_addr := 0x10001000.U(32.W)// rx0
-          apb_wdata := 0.U(32.W)
-          apb_write := false.B
+        is(xip_wait) {
+          when((apb_state === idle) && (apb_rdata(8) === 1.U) || (apb_state === enable && apb_bus.pready && apb_bus.prdata(8) === 1.U)){
+            state := xip_wait
+            apb_run := true.B
+          }.elsewhen((apb_state === idle) && (apb_rdata(8) === 0.U) || (apb_state === enable && apb_bus.pready && apb_bus.prdata(8) === 0.U)){
+            state := xip_read
+            apb_run := true.B
+            apb_addr := 0x10001000.U(32.W)// rx0
+            apb_wdata := 0.U(32.W)
+            apb_write := false.B
+          }
         }
-      }
-      is(xip_read) {
-        when(apb_state === idle || apb_state === enable && apb_bus.pready){
-          state := normal
-          in.pready := true.B
-          in.prdata := Mux(apb_state === idle,Cat(apb_rdata(7,0),apb_rdata(15,8),apb_rdata(23,16),apb_rdata(31,24)),
-                                              Cat(apb_bus.prdata(7,0),apb_bus.prdata(15,8),apb_bus.prdata(23,16),apb_bus.prdata(31,24)))
+        is(xip_read) {
+          when(apb_state === idle || apb_state === enable && apb_bus.pready){
+            state := normal
+            in.pready := true.B
+            in.prdata := Mux(apb_state === idle,Cat(apb_rdata(7,0),apb_rdata(15,8),apb_rdata(23,16),apb_rdata(31,24)),
+                                                Cat(apb_bus.prdata(7,0),apb_bus.prdata(15,8),apb_bus.prdata(23,16),apb_bus.prdata(31,24)))
+          }
         }
       }
-    }
 
-    apb_bus.paddr   := apb_addr
-    apb_bus.psel    := false.B
-    apb_bus.penable := false.B
-    apb_bus.pprot   := 1.U(3.W)
-    apb_bus.pwrite  := apb_write
-    apb_bus.pwdata  := apb_wdata
-    apb_bus.pstrb   := 0xf.U(4.W)
-    switch(apb_state) {
-      is(idle) {
-        when(apb_run) {
-          apb_state := setup
+      apb_bus.paddr   := apb_addr
+      apb_bus.psel    := false.B
+      apb_bus.penable := false.B
+      apb_bus.pprot   := 1.U(3.W)
+      apb_bus.pwrite  := apb_write
+      apb_bus.pwdata  := apb_wdata
+      apb_bus.pstrb   := 0xf.U(4.W)
+      switch(apb_state) {
+        is(idle) {
+          when(apb_run) {
+            apb_state := setup
+          }
         }
-      }
-      is(setup) {
-        apb_state := enable
-        apb_bus.psel := true.B
-      }
-      is(enable) {
-        when(apb_bus.pready && !apb_run) {
-          apb_state := idle
-        }.elsewhen(apb_bus.pready && apb_run) {
-          apb_state := setup
+        is(setup) {
+          apb_state := enable
+          apb_bus.psel := true.B
+        }
+        is(enable) {
+          when(apb_bus.pready && !apb_run) {
+            apb_state := idle
+          }.elsewhen(apb_bus.pready && apb_run) {
+            apb_state := setup
+          }
+          apb_bus.psel := true.B
+          apb_bus.penable := true.B
+          apb_rdata := apb_bus.prdata
         }
-        apb_bus.psel := true.B
-        apb_bus.penable := true.B
-        apb_rdata := apb_bus.prdata
       }
-      // is(get_data) {
-      //   apb_rdata := apb_bus.prdata
-      //   apb_state := idle
-      // }
-    }
 
+    }
     
   }
 }
-- 
2.34.1

